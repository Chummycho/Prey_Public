
const weapon_trigger = false
EntityEvents.hurt(event => {
    //let x = event.entity.x
    //let y = event.entity.y
   // let z = event.entity.z
   // Utils.server.runCommandSilent(`/setblock ${x} ${y} ${z} minecraft:anvil`)
    //event.level.setBlock('minecraft:anvil',)
    //if (!event.source.player) return
    //event.entity.potionEffects.add("undead_unleashed:flame_breathing", 50)
    //if (!weapon_trigger) return
    //console.log(event.entity.nbt)
   // Utils.server.tell(`Immediate: ${event.source.immediate.type}`)
   // Utils.server.tell(`Actual: ${event.source.actual.type}`)
    //Utils.server.tell(`Type: ${event.source.immediate.displayName.getString()}`)
    //Utils.server.tell('')
    //Utils.server.tell(time)
    //if (!event.source.player) return
    //Utils.server.tell(event.source.type)
   // console.log(event.entity.nbt)
})




